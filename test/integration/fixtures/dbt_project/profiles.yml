test_project:
  target: dev
  outputs:
    dev:
      type: duckdb
      # a database file per test run (the harness sets DUCKDB_PATH); one process at a time holds it
      path: "{{ env_var('DUCKDB_PATH') }}"
      threads: 1
