# The METRICFLOW environment: MetricFlow's `mf` and the Python dbt-core + adapter it queries the
# warehouse with (DuckDB here; requirements-metricflow-bigquery.txt for BigQuery). dbt's docs for a
# setup without the dbt platform: "install MetricFlow separately and use the mf prefix" — it reads the
# semantic_manifest.json whichever dbt environment ran `dbt parse` wrote. It lives in a venv of its own
# because dbt-core installs a `dbt` command that would replace a dbt v2 binary in a shared venv.
#   * dbt-metricflow 0.13.0 pins metricflow 0.211.0 and caps dbt-core < 1.12 (the version pairing
#     between dbt and MetricFlow is ours to manage, per dbt's docs).
dbt-metricflow==0.13.0
dbt-core==1.11.11
dbt-duckdb>=1.10,<1.12
