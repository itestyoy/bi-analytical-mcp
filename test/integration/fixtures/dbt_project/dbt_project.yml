name: "test_project"
version: "1.0.0"
config-version: 2
profile: "test_project"
model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
models:
  test_project:
    +materialized: table
